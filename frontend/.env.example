# Environment variables for the frontend application (Vite)
# Copy this file to .env, .env.local, .env.development, .env.production etc. in the frontend/ directory.
# Only variables prefixed with VITE_ are exposed to the client-side bundle.
# See https://vitejs.dev/guide/env-and-mode.html

VITE_APP_TITLE="Zebra Printing"
